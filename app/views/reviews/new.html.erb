<h4>Form</h4>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: 0..5 %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>

<br><br>
<%= link_to 'Go back to ' + @restaurant.name, restaurant_path(@restaurant.id) %>
